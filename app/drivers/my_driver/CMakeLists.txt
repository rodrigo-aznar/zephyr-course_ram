if (CONFIG_MY_DRIVER)
    target_sources(app PRIVATE my_driver.c)
    target_include_directories(app PRIVATE .)
    if (CONFIG_SHELL)
        target_sources(app PRIVATE my_driver_shell.c)
    endif()
endif()
